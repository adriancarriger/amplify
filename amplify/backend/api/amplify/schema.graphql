type Contact @model @auth(rules: [{ allow: owner, operations: [create, update, delete, read] }]) {
  id: ID!
  firstName: String
  lastName: String
  phones: [Phone]
}

type Phone {
  number: String!
  type: String
}
